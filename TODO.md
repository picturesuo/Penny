# TODO

- Add real domain models and routing once the minimal restart is stable.
- Introduce tests for the API health route and frontend health UI.
- Add CI for install, lint, typecheck, and build.
- Decide deployment targets for `apps/web` and `apps/api`.
